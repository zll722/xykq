/* ---- 头部渐变卡片 ---- */
.profile-header {
  text-align: center;
  padding: 56rpx 32rpx 48rpx;
  background: linear-gradient(145deg, #1a5fc8, #409EFF, #69c0ff) !important;
  border-radius: 28rpx !important;
  position: relative;
  overflow: hidden;
}

.profile-header::before {
  content: '';
  position: absolute;
  top: -60rpx;
  right: -60rpx;
  width: 280rpx;
  height: 280rpx;
  border-radius: 50%;
  background: rgba(255,255,255,0.08);
}

.profile-header::after {
  content: '';
  position: absolute;
  bottom: -80rpx;
  left: -40rpx;
  width: 220rpx;
  height: 220rpx;
  border-radius: 50%;
  background: rgba(255,255,255,0.06);
}

.avatar-wrap {
  position: relative;
  display: inline-block;
  margin-bottom: 24rpx;
}

.avatar-placeholder {
  width: 152rpx;
  height: 152rpx;
  border-radius: 50%;
  background: rgba(255,255,255,0.25);
  border: 4rpx solid rgba(255,255,255,0.7);
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 8rpx 32rpx rgba(0,0,0,0.15);
}

.avatar-text {
  font-size: 60rpx;
  color: #fff;
  font-weight: 800;
}

.avatar-img {
  width: 152rpx;
  height: 152rpx;
  border-radius: 50%;
  border: 4rpx solid rgba(255,255,255,0.7);
  box-shadow: 0 8rpx 24rpx rgba(0,0,0,0.15);
}

.avatar-edit-tag {
  position: absolute;
  bottom: 0;
  left: 50%;
  transform: translateX(-50%);
  background: rgba(0,0,0,0.45);
  color: #fff;
  font-size: 20rpx;
  padding: 4rpx 18rpx;
  border-radius: 12rpx;
  white-space: nowrap;
}

.profile-name {
  display: block;
  font-size: 42rpx;
  font-weight: 800;
  color: #fff;
  margin-bottom: 12rpx;
  text-shadow: 0 2rpx 8rpx rgba(0,0,0,0.12);
  position: relative;
  z-index: 1;
}

.profile-role-tag {
  display: inline-block;
  background: rgba(255,255,255,0.22);
  color: #fff;
  font-size: 24rpx;
  padding: 6rpx 28rpx;
  border-radius: 20rpx;
  margin-bottom: 16rpx;
  border: 1rpx solid rgba(255,255,255,0.4);
  position: relative;
  z-index: 1;
}

.profile-username {
  display: block;
  font-size: 24rpx;
  color: rgba(255,255,255,0.7);
  position: relative;
  z-index: 1;
}

/* ---- 编辑表单 ---- */
.edit-form {
  padding: 32rpx;
}

.form-title {
  display: block;
  font-size: 30rpx;
  font-weight: 700;
  color: #1a1a2e;
  margin-bottom: 28rpx;
  padding-left: 16rpx;
  border-left: 5rpx solid #409EFF;
}

.form-item {
  margin-bottom: 24rpx;
}

.form-label {
  display: block;
  font-size: 24rpx;
  color: #8c9dae;
  margin-bottom: 12rpx;
  font-weight: 500;
}

.form-input {
  width: 100%;
  height: 84rpx;
  background: #f5f7fb;
  border-radius: 14rpx;
  padding: 0 24rpx;
  font-size: 28rpx;
  color: #2c2c3e;
  box-sizing: border-box;
  border: 2rpx solid transparent;
}

.btn-save {
  width: 100%;
  height: 88rpx;
  background: linear-gradient(135deg, #2471d6, #409EFF);
  color: #fff;
  border: none;
  border-radius: 18rpx;
  font-size: 30rpx;
  font-weight: 600;
  margin-top: 16rpx;
  box-shadow: 0 8rpx 24rpx rgba(64, 158, 255, 0.4);
  letter-spacing: 2rpx;
}

.btn-save::after { border: none; }

/* ---- 菜单列表 ---- */
.menu-list {
  margin-top: 8rpx;
}

.menu-item {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 28rpx 32rpx;
  font-size: 30rpx;
  border-radius: 20rpx;
}

.menu-left {
  display: flex;
  align-items: center;
  gap: 24rpx;
}

.menu-icon-box {
  width: 72rpx;
  height: 72rpx;
  border-radius: 18rpx;
  background: linear-gradient(135deg, #409EFF, #2471d6);
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 4rpx 14rpx rgba(64,158,255,0.3);
}

.menu-icon {
  font-size: 32rpx;
  color: #fff;
  width: auto;
  text-align: center;
}

.menu-label {
  font-size: 30rpx;
  color: #2c3e50;
  font-weight: 500;
}

.menu-arrow {
  color: #d0dae4;
  font-size: 44rpx;
  font-weight: 300;
}

.logout-item {
  justify-content: center;
  border: 1rpx solid #ffe8e8;
  background: #fff8f8;
}

.logout-text {
  color: #F56C6C;
  font-size: 30rpx;
  font-weight: 600;
}
