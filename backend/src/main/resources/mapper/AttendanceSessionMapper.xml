<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campus.attendance.mapper.AttendanceSessionMapper">

    <resultMap id="AttendanceSessionMap" type="com.campus.attendance.domain.AttendanceSession">
        <id property="id" column="id"/>
        <result property="scheduleId" column="schedule_id"/>
        <result property="courseId" column="course_id"/>
        <result property="classId" column="class_id"/>
        <result property="attendanceDate" column="attendance_date"/>
        <result property="signType" column="sign_type"/>
        <result property="status" column="status"/>
        <result property="startTime" column="start_time"/>
        <result property="endTime" column="end_time"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO attendance_session (schedule_id, course_id, class_id, attendance_date, sign_type, status, start_time, end_time)
        VALUES (#{scheduleId}, #{courseId}, #{classId}, #{attendanceDate}, #{signType}, #{status}, #{startTime}, #{endTime})
    </insert>

    <update id="update">
        UPDATE attendance_session
        SET status = #{status}, end_time = #{endTime}
        WHERE id = #{id}
    </update>

    <select id="findById" resultMap="AttendanceSessionMap">
        SELECT * FROM attendance_session WHERE id = #{id}
    </select>

    <select id="findActiveSession" resultMap="AttendanceSessionMap">
        SELECT * FROM attendance_session
        WHERE schedule_id = #{scheduleId}
          AND attendance_date = #{attendanceDate}
          AND sign_type = #{signType}
          AND status = 'OPEN'
        LIMIT 1
    </select>

    <select id="findSessionsByScheduleAndDate" resultMap="AttendanceSessionMap">
        SELECT * FROM attendance_session
        WHERE schedule_id = #{scheduleId}
          AND attendance_date = #{attendanceDate}
    </select>

</mapper>
